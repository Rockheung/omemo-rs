# Self-signed certs are regenerated per operator (network-specific
# SAN entries) and never committed.
cert.pem
key.pem

# Likewise the Prosody sidecar config that mirrors the SAN list as
# stub VirtualHosts is regenerated by `gen-cert.sh` (kept in
# `../prosody/extra-vhosts.cfg.lua`; gitignored from there too).
